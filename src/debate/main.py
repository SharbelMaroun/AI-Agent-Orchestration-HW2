"""CLI entry point — terminal menu for running debates.

Implementation pending Phase 7.1. For now, a stub `cli()` placeholder so the
`debate` script registered in pyproject.toml is importable.
"""


def cli() -> None:
    """Launch the terminal menu. To be implemented in Phase 7.1."""
    print("AI Agent Debate — CLI not yet implemented. See docs/TODO.md Phase 7.1.")


if __name__ == "__main__":
    cli()
